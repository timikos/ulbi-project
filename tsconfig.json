{
  "compilerOptions": {
    "outDir": "./dist/", // Куда производится сборка (неособа важна, тк мы собираем с помощью вебпака)
    "noImplicitAny": true, // Не позволяет использовать переменные без явного указания типа
    "module": "esnext", // Модульная система
    "target": "es5", // В какую версию будет компилироваться
    "jsx": "react-jsx", // Чтобы можно было писать ДЖСХ конструкции и не писать импорта Реакта
    "allowJs": true, // Позволяет обрабатывать не только тс, но и джс
    "moduleResolution": "node", // Определяет какие импорты будут
    "baseUrl": ".", // Для абсолютного импорта
    "esModuleInterop": true, // Позволяет работать с пакетами common js как с обычными пакетами с помощью import
    "allowSyntheticDefaultImports": true, // Делает более привычные импорты
  },
  "ts-node": { // Чтоб импорты заработали
    "compilerOptions": {
      "module": "CommonJS"
    }
  }
}
